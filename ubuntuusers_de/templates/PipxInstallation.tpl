## Macro=[[Vorlage(PipxInstallation, %%Python-Modul%%)]]
[:pipx:], als [wikipedia:Wrapper_(Software):Wrapper-Programm] des Python-Paketmanagers [:pip:], stellt eine einfache Möglichkeit dar, in Python geschriebene Applikationen in [:venv:isolierten Umgebungen] zu installieren:

{{|<class="bash">{{{pipx install <@ $arguments @>       # Programm wird nur für den aktuellen Nutzer installiert  }}}|}}
